{
  "name": "musig-banano-wrapper",
  "version": "1.0.2",
  "description": "## Wrapper for the musig-banano-wasm",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc --build tsconfig.json",
    "test": "mocha --require ts-node/register ./test/*.spec.ts",
    "awesometest": "mocha -R mochawesome --require ts-node/register ./test/*.spec.ts"
  },
  "author": "Airtune",
  "license": "MIT",
  "files": [
    "dist/**/*.*",
    "src/**/*.*"
  ],
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "@types/node": "^20.2.5",
    "chai": "^4.3.7",
    "chai-as-promised": "^7.1.1",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "@bananocoin/bananojs": "^2.8.8",
    "mochawesome": "^7.1.3",
    "musig-banano-wasm": "file:../pkg"
  }
}
