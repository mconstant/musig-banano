{
  "name": "musig-banano",
  "version": "1.0.1",
  "description": "## Building the wasm",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc --build tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Airtune",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.2.5"
  },
  "dependencies": {
    "typescript": "^5.1.3"
  }
}
